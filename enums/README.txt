TODO : Fix pour supporter "a = b" (Les espaces ne fonctionnent pas

NOTE : Quand on recompile le fichier .cc, il faut remettre public les IDs dans ValuesWith (juste autocorrect le fichier interpretor.java)